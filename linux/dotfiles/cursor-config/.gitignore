# Machine-specific directories (if accidentally created here)
globalStorage/
workspaceStorage/
History/

# Profile directories use random hashes per-machine - don't track them
# Settings are stored in PeterProfile/ and symlinked at install time
profiles/
